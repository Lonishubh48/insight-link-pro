# Insight-Link Pro — Python dependencies
# Install with: pip install -r requirements.txt

# MCP framework
fastmcp>=2.0.0

# Async HTTP client
httpx>=0.27.0

# Environment variable loading
python-dotenv>=1.0.0

# Data validation (optional, used in pydantic-compatible validators)
pydantic>=2.0.0

# Development / testing (not required at runtime)
pytest>=8.0.0
pytest-asyncio>=0.23.0